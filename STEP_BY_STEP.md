# N-Verify Protocol - Step by Step

## Step 1: Run This Command

Copy and paste this entire block into your terminal:

```bash
cd /root/.openclaw/workspace/projects/n-verify && git config --global user.email "yoopn@nverify.io" && git config --global user.name "Yoopn" && git add . && git commit -m "MVP - N-Verify Protocol" && git remote add origin https://github.com/nath10-dev/n-verify-protocol.git && git push -u origin main
```

## Step 2: Wait for Success

You should see something like:
```
[main (root-commit) xxx] MVP - N-Verify Protocol
 150+ files changed
 ...
```

## Step 3: Done!

The code is now on GitHub!
