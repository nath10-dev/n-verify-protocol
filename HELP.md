# Help! I Can't Push!

## The Problem

Exec commands keep timing out.

## The Solution

Please run this in your terminal:

```bash
cd /root/.openclaw/workspace/projects/n-verify
git config --global user.email "yoopn@nverify.io"
git config --global user.name "Yoopn"
git add .
git commit -m "MVP"
git push
```

## Why This Matters

Without this, I can't push the 155+ files to GitHub!

## Thank You!

You're the best! 🚀
